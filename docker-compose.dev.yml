services:
  clickhouse:
    image: clickhouse/clickhouse-server:25.10
    container_name: genum-clickhouse
    environment:
      - CLICKHOUSE_DB=${CLICKHOUSE_DB:-default}
      - CLICKHOUSE_USER=${CLICKHOUSE_USER:-clickhouse}
      - CLICKHOUSE_PASSWORD=${CLICKHOUSE_PASSWORD:-clickhouse}
    ports:
      - "127.0.0.1:8123:8123"  # HTTP interface
      - "127.0.0.1:9000:9000"  # Native protocol
    volumes:
      - genum-clickhouse_data:/var/lib/clickhouse
      - genum-clickhouse_logs:/var/log/clickhouse-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8123/ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - default

  postgres:
    image: docker.io/postgres:17
    container_name: genum-postgres
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 3s
      timeout: 3s
      retries: 10
    command: ["postgres", "-c", "log_statement=all"]
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - POSTGRES_DB=${POSTGRES_DB:-postgres}
      - TZ=UTC
      - PGTZ=UTC
    ports:
      - 127.0.0.1:5432:5432
    volumes:
      - genum-postgres_data:/var/lib/postgresql/data
    networks:
      - default

  minio:
    image: minio/minio:latest
    container_name: genum-minio
    entrypoint: sh
    command: -c 'mkdir -p /data/genum && minio server --address ":9000" --console-address ":9001" /data' # add genum bucket and start MinIO server
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minio}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-miniosecret}
    ports:
      - 127.0.0.1:9090:9000
      - 127.0.0.1:9091:9001 # console
    volumes:
      - genum-minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 1s
      timeout: 5s
      retries: 5
      start_period: 1s
    networks:
      - default

volumes:
  genum-clickhouse_data:
  genum-clickhouse_logs:
  genum-postgres_data:
  genum-minio_data:

networks:
  default:
    driver: bridge
